export interface Cliente {
    id:string;
    nome: string;
    senha: string;
    email: string;
    }
export interface Resposta {
    idPergunta: number;
    resposta: boolean;
}     
export interface Horario {
    idPergunta: number;
    resposta: boolean;
}